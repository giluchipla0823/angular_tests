export default class Patterns {
  static ALPHANUMERIC = /^\w+$/;
  // static NUMBERS = /^-?(0|[1-9]\d*)?$/;
  static NUMBERS = /^[0-9]+$/;
  static PHONE = /^[0-9]+$/;
  static TWO_DECIMALS = /^\d+\.\d{2}$/;
  static DECIMALS = /^[.\d]+$/;
}
